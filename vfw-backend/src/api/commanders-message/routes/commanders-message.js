'use strict';

/**
 * commanders-message router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::commanders-message.commanders-message');
